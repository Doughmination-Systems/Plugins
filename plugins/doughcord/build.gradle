description = "Doughcord Plugin - Feature-rich Minecraft plugin"

dependencies {
    compileOnly project(':doughapi')
}

tasks.named('compileJava') {
    dependsOn ':doughapi:shadowJar'
}

shadowJar {
    archiveBaseName.set('Doughcord')
}