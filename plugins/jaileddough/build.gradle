description = "A jail plugin for Minecraft servers"

dependencies {
    compileOnly project(':doughapi')
}

tasks.named('compileJava') {
    dependsOn ':doughapi:shadowJar'
}

shadowJar {
    archiveBaseName.set('JailedDough')
}